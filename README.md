E-commerce Sales Dashboard (Power BI End-to-End Project)

Этот проект —  BI-кейс, демонстрирующий процесс анализа данных от "сырых" CSV-файлов до готового интерактивного дашборда.

## 🏁 Цель проекта

Анализ данных e-commerce магазина для выявления ключевых показателей эффективности (KPI), таких как общая выручка, прибыль, маржинальность, а также определение самых прибыльных категорий товаров и регионов.

## 🛠 Используемые инструменты

* **Power BI** (Power Query, DAX)
* **SQL** (PostgreSQL)

---

## 🚀 Ключевые навыки

### 1. ETL (Power Query)

* Загрузка и очистка "грязных" данных из CSV-файлов.
* **Преобразование типов:** Работа с "грязными" числовыми (напр., `"1,500.90"`) и текстовыми форматами.
* **Очистка данных:** Обработка `null` и пустых строк.
* **Работа с локалями:** Решение проблемы с разделителями (запятая/точка) через `Using Locale`.

### 2. Моделирование данных (Data Modeling)

* Построение реляционной модели данных "Звезда" (Star Schema).
* Настройка связей "Один-ко-Многим" (1:*) между таблицами фактов (`fact_Sales`) и измерений (`dim_Customers`, `dim_Products`).

### 3. DAX (Data Analysis eXpressions)

* Создание вычисляемых мер (Measures) для расчета KPI:
    * `Total Revenue` (Общая выручка) - с использованием `SUMX`.
    * `Total Cost` (Общая себестоимость) - с использованием `SUMX` и `RELATED`.
    * `Total Profit` (Общая прибыль).
    * `Profit Margin %` (Маржа прибыли) - с использованием `DIVIDE`.

### 4. SQL (Оконные функции)

* Написание аналитических запросов для анализа поведения клиентов (см. папку `/sql/`).
* Использование `LAG()` для поиска предыдущей сессии/покупки.
* Использование `ROW_NUMBER()` для ранжирования транзакций.

---

## 📊 Финальный дашборд

*Здесь ты вставляешь свой скриншот. В GitHub Markdown это делается так:*
![My Dashboard](images/dashboard_screenshot.png)
